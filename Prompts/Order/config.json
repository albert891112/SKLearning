{
    "schema": 1,
    "type": "compLetion",
    "description": "Create a response that can determine which product a user ordered",
    "execution_settings":   {
        "default" : {
            "max_tokens": 4000,
            "temperature": 0.0,
            "top_p": 0.0,
            "presence_penalty": 0.0,
            "frequency_penalty": 0.0
        }
    },
    "input_variable": {
        "name": "order",
        "description" : "The order that the user placed",
        "type": "string",
        "requiered": true
    }
}
